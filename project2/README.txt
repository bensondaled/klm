Place data directory into `inputs' folder.
It should be a folder named with the date of the acquisition, containing a group of folders, like t1, t2, t3, etc.
Those folders should contain tif files, each of which has a single image with 2 channels.

Run `pipeline.py <inputs/folder_of_interest>'
